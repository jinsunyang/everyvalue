<%= form_with(model: subject_attachment, local: true) do |form| %>
  <% if subject_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject_attachment.errors.count, "error") %> prohibited this subject_attachment from being saved:</h2>

      <ul>
      <% subject_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subject_id %>
    <%= form.text_field :subject_id, id: :subject_attachment_subject_id %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, id: :subject_attachment_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
